# Incremental dependencies for the LiveKit fallback voice channel only.
# fastapi, uvicorn (via fastapi[standard]), python-dotenv, and redis are already
# satisfied by backend/requirements.txt - not repeated here to avoid version drift.
#
# Install with (from inside backend/):
#   pip install -r livekit_agent/requirements.txt

livekit-agents
livekit-api
livekit-plugins-google

# No longer required now that worker.py uses Gemini's realtime model for STT+TTS too -
# left here in case you revert to a Deepgram/Cartesia pipeline later.
# livekit-plugins-deepgram
# livekit-plugins-cartesia
# livekit-plugins-silero
