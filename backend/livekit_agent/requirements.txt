# Incremental dependencies for the LiveKit fallback voice channel only.
# fastapi, uvicorn (via fastapi[standard]), python-dotenv, and redis are already
# satisfied by backend/requirements.txt - not repeated here to avoid version drift.
#
# Install with (from inside backend/):
#   pip install -r livekit_agent/requirements.txt

livekit-agents
livekit-api
livekit-plugins-elevenlabs
livekit-plugins-google
livekit-plugins-openai
livekit-plugins-silero

# livekit-plugins-openai above is used for STT (pointed at Groq's OpenAI-compatible
# endpoint, not OpenAI directly - see config.py); livekit-plugins-google above is used
# for the Gemini text LLM only, not STT.
#
# No longer required - Deepgram STT (Nova-2/Nova-3 don't support Tamil at all, confirmed
# via Deepgram's own docs), Google Cloud STT (persistent IAM permission error), and
# Cartesia TTS (replaced by ElevenLabs earlier) aren't used anymore.
# livekit-plugins-turn-detector was tried but needs a one-time model download AND is now
# deprecated - removed rather than add setup friction. Left all here in case you
# revert/re-add any of these.
# livekit-plugins-deepgram
# livekit-plugins-cartesia
# livekit-plugins-turn-detector
